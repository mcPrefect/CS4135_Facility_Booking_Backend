package com.facilitybooking.approvalservice.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // Placeholder for shared beans if needed in future iterations.
    // RestTemplate not required here as the Approval Service is purely event-driven
    // and does not make synchronous REST calls to other services.
}
